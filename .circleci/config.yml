version: 2.1

orbs:
  android: circleci/android@2.3.0

jobs:
  build:
    docker:
      - image: cimg/android:2023.09
    steps:
      - run: echo "I'm job named build try - Build job with in  a workflow named build"


workflows:
  build-workflow:
    jobs:
      - build